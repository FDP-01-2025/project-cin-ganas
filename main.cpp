#include <iostream>
using namespace std;
#include "src/StartScreen.h"
#include "src/Pet.h"

main()
{
    // Show the StartScreen (the game title + the main menu)
    ShowStartScreen();

    return 0;
}
